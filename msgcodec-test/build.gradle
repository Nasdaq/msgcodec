description = 'JUnit test suite for msgcodec format providers'

dependencies {
  compile project(':msgcodec')
  compile group: 'junit', name: 'junit', version:junit_version
  //  testCompile group: 'junit', name: 'junit', version:junit_version
}
