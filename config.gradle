// Gradle configurations for this module
ext {
    msgcodec = [
      'version' : "git describe".execute().text.trim().replaceFirst(/-([0-9]+-g.*)$/, '+$1'),
         'vcs' :  [
            'rootDevelopment' : 'git@gitrepo.cinnober.com:clearing/msgcodec.git',
            'rootCompiled' : 'default',
        ],

        // Projects that belong to this module
        'projects' : [
                'msgcodec',
                'msgcodec-test',
                'msgcodec-blink',
                'msgcodec-examples',
                'msgcodec-javadoc',
                'msgcodec-json',
                'msgcodec-tap',
                'msgcodec-xml'
        ],

    ]
}
